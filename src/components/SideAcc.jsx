{/* import {useSelector} from 'react-redux'
import {selectUser} from "../features/userSlice";

function SideAcc() {
    const user = useSelector(selectUser)
    return (
        <img src={user.photoUrl} className={'sidebar_avatar'} alt={'user image'}/>
    )
}

export default SideAcc

 */}